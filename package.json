{
  "name": "handmade-bliss-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for the Handmade Bliss e-commerce application",
  "private": true,
  "scripts": {
    "install:deps": "python -m pip install -r requirements.txt",
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "python -m compileall ."
  },
  "keywords": [
    "fastapi",
    "python",
    "sqlalchemy",
    "api",
    "backend"
  ],
  "author": "Handmade Bliss",
  "license": "MIT"
}
